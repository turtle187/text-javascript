var bp = new Array("head", "finger" ,"toe" ,"knee");
 //bp.reverse
 //add to array
 bp.push("leg", "foot");
// to sort in order
bp.sort();
 // takes array and converts it to a string
var string1 = bp.join();

console.log(string1);